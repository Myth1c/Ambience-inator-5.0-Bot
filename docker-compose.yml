services:
  ambience-bot:
    build: .
    container_name: ambience-inator-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data    # optional, if you have data files
    command: ["python3", "bot_runner.py"]
